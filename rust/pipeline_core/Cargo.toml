[package]
name = "pipeline_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "pipeline_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
hound = "3.5"
rand = "0.8"

[profile.release]
opt-level = 3
lto = "thin"
